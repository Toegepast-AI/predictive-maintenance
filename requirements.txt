# CPU-only optimized requirements for smaller Docker image
flask==3.0.0
numpy==1.24.3
pillow==10.1.0

# PyTorch CPU-only (much smaller than CUDA version)
# Install CPU-only versions using --index-url
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.1+cpu
torchvision==0.16.1+cpu

# Transformers with minimal dependencies
transformers==4.35.2
tokenizers==0.15.0
huggingface-hub==0.19.4
safetensors==0.4.1

# Production server
gunicorn==21.2.0
